<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.andriaware.civicsense">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".CreaSegnalazioneActivity"
            android:label="Crea segnalazione" />
        <activity
            android:name=".MainActivity"
            android:label="Segnalazioni" />
        <activity
            android:name=".Cerca"
            android:label="@string/title_activity_cerca" />
        <activity
            android:name=".Profilo"
            android:label="@string/title_activity_profilo" />
        <activity android:name=".DettaglioSegnalazione" />
        <activity android:name=".VisualizzaFotoActivity" />
        <activity android:name=".ModificaSegnalazione"></activity>
    </application>

</manifest>